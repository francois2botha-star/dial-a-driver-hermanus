{
  "name": "dial-a-driver-hermanus",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "node scripts/generate-sitemap.cjs",
    "preview": "vite preview",
    "generate-report": "node scripts/generate-ga4-report.js",
    "deploy": "npm run build && git add dist -f && git commit -m \"Deploy to GitHub Pages\" && git subtree push --prefix dist origin gh-pages"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-helmet": "^6.1.0",
    "react-router-dom": "^7.13.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@vitejs/plugin-react": "^4.2.1",
    "googleapis": "^121.0.0",
    "sharp": "^0.34.5",
    "vite": "^5.0.8"
  }
}
